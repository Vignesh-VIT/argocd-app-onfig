apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.boschdevcloud.com/SW-PMT-PlayArea/argocd-app-config.git
    targetRevision: HEAD
    path: dev
  destination: 
    server: https://51.124.72.178:8080
    namespace: myapp

  syncPolicy:git 
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
